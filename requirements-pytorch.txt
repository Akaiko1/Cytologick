# Cytologick - PyTorch Installation (Recommended)
# =================================================
# Install with: pip install -r requirements-pytorch.txt
#
# IMPORTANT: GPU/CUDA Support
# ---------------------------
# This file installs CPU-only PyTorch by default.
# For GPU acceleration, install PyTorch separately using the official command builder:
#
#   https://pytorch.org/get-started/locally/
#
# Example commands for CUDA:
#   CUDA 12.4: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Then install the rest: pip install -r requirements-pytorch.txt
# (PyTorch will be skipped if already installed)

# ---------------------------------
# Base dependencies
# ---------------------------------
-r requirements.txt

# ---------------------------------
# PyTorch (CPU version - see above for GPU)
# ---------------------------------
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ---------------------------------
# Segmentation & Augmentation
# ---------------------------------
# segmentation-models-pytorch 0.3.4+ vendors encoders (no need for efficientnet-pytorch, pretrainedmodels)
segmentation-models-pytorch>=0.3.4
albumentations>=1.4.0

# timm is used by smp for additional encoders
timm>=0.9.0
